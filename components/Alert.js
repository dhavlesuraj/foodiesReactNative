import React from 'react'
import { View } from 'react-native-web'

const Alert = ({message}) => {
  return (
    <View>
    <Alert
      message={message}
    />
    </View>
  )
}

export default Alert
